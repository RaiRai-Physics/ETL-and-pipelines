# Runbook

Run `bash run_python_pipeline.sh`. Then review `reports/python/data_quality_report.csv`, `reports/python/pipeline_audit_log.csv`, and `metadata/run_watermarks.json`.

Common investigations start in `data/quarantine/python/` and `data/gold/python/exception_*.csv`.
